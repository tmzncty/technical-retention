name: integrate-case43-avatar

on:
  push:
    branches: [main]

permissions:
  contents: write

jobs:
  integrate:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Integrate grounded AVATAR case
        shell: bash
        run: |
          python - <<'PY'
          from pathlib import Path

          case = r'''# AVATAR VRT-Aware DRAM Refresh: ECC Feedback, Scrubbing, and Runtime Row Reclassification

## Status

**`grounded`** — bounded to Qureshi et al.'s DSN 2015 AVATAR research design and evaluation. The case uses the paper's experimental characterization of variable-retention-time behavior in 24 DRAM chips to motivate a system-level controller design in which ECC-corrected runtime failures and proactive scrubbing can revise a row's refresh class.

Grounding record: [`../evidence/43-avatar-2015-vrt-aware-refresh-grounding.md`](../evidence/43-avatar-2015-vrt-aware-refresh-grounding.md).

## Scope

This case asks the narrow question left open by Case 40:

> What changes when a retained refresh profile is **revised at runtime by observed correctable failures**, rather than being trusted as a static description of row retention behavior?

AVATAR (`A Variable-Retention-Time (VRT) Aware Refresh for DRAM Systems`) combines:

1. initial retention testing that populates a `Row Refresh Table` (`RRT`);
2. multirate refresh with `SlowRefresh` and `Fast Refresh` classes;
3. ECC on ordinary accesses;
4. proactive memory scrubbing to inspect low-activity memory;
5. runtime row upgrades when ECC detects and corrects an error;
6. infrequent retention testing that can later downgrade rows again.

This is **not**:

- a full history of VRT physics or DRAM retention profiling;
- a claim that AVATAR shipped in a commercial controller;
- a JEDEC refresh-standard genealogy;
- a claim that ECC and refresh are one operation;
- a claim that scrubbing and refresh are one operation;
- a complete solution to data-pattern dependence (`DPD`);
- a RowHammer mitigation case;
- evidence that the modeled reliability numbers are field lifetimes of deployed hardware.

The bounded contribution is a **runtime maintenance-policy feedback regime**: observed correctable failures can become evidence that changes how frequently the affected row will be maintained in the future.

## Relation to Case 40

Case 40 establishes two facts that matter here:

```text
retention profile can be retained across time
    !=
retention profile remains conservative across time
```

The 2013 DPD/VRT evidence showed why a profile can survive perfectly while the relation it describes becomes incomplete or non-conservative.

Case 43 does not erase that problem. It changes the response:

```text
initial profile
    -> runtime ECC observation
    -> correction of current error
    -> row-class update
    -> faster future refresh
    -> proactive scrub for cold memory
    -> later retention re-test / possible downgrade
```

The changed relation is therefore:

> **maintenance-policy metadata can itself be maintained through runtime feedback.**

## Historical vocabulary and record

Moinuddin K. Qureshi, Dae-Hyun Kim, Samira Manabi Khan, Prashant J. Nair, and Onur Mutlu presented **“AVATAR: A Variable-Retention-Time (VRT) Aware Refresh for DRAM Systems”** at the 45th Annual IEEE/IFIP International Conference on Dependable Systems and Networks (DSN), 2015, pp. 427–437, DOI `10.1109/DSN.2015.58`.

The paper's own vocabulary includes:

- `AVATAR`;
- `Variable-Retention-Time (VRT) Aware Refresh`;
- `Active-VRT Pool (AVP)`;
- `Active-VRT Injection (AVI)`;
- `Row Refresh Table (RRT)`;
- `SlowRefresh`;
- `Fast Refresh`;
- `Data ECC`;
- `Scrub`;
- `Retention Testing`.

The authors experimentally characterize VRT behavior using **24 DRAM chips**, then use those observations to drive an architecture-level reliability/performance evaluation. The paper reports that new VRT-related failures can continue to appear after long testing, which is the reason it rejects reliance on a one-time static profile alone.

The paper's evaluation numbers are retained as **research-model results**, not commercial deployment measurements.

## Mechanism

### 1. Initial testing creates a row-refresh policy state

AVATAR begins with retention testing and stores the resulting row classification in the `Row Refresh Table`.

A row classified as ordinary can use the slower refresh regime. Rows known or later observed to require stronger protection can be placed in the faster regime.

The RRT is therefore not payload. It is retained **control state about how the payload should be maintained**.

### 2. ECC corrects a manifested error and supplies feedback

During normal accesses, AVATAR uses ECC to detect and correct a correctable data error. When a word in a row produces such an event, AVATAR upgrades the **row** to `Fast Refresh`.

This separates two actions:

```text
correct the value that is wrong now
    !=
change the maintenance policy intended to reduce future errors
```

Therefore:

> **error correction ≠ future protection**.

The first restores the currently observed logical value. The second changes the future refresh cadence for a larger spatial unit.

### 3. Demand accesses do not cover cold memory

ECC on ordinary reads/writes observes only lines that are actually accessed. A row that becomes vulnerable while remaining cold can therefore escape demand-triggered observation.

AVATAR adds periodic **memory scrub** to inspect all memory, correct detected errors, and feed the same row-upgrade path.

Therefore:

> **demand-triggered ECC observation ≠ full-memory coverage**.

And:

> **memory scrub ≠ DRAM refresh**.

Scrub is a verification/detection/correction traversal. Refresh is the charge-restoration operation whose cadence AVATAR changes. They cooperate in one retention policy but remain different work.

### 4. Conservative evidence is not perfectly specific evidence

The paper notes that a correctable ECC event can also be caused by a soft error. AVATAR can therefore conservatively upgrade a row even when the observed error was not caused by VRT.

So:

> **ECC correction event ≠ certain VRT diagnosis**.

In this design, the safe direction is to spend extra refresh work rather than risk leaving a truly weak row under-refreshed.

### 5. Runtime upgrade need not be permanent

If upgrades only accumulate, VRT observations can gradually move more rows into the fast class and erode refresh savings. AVATAR therefore includes infrequent retention testing — evaluated with a yearly interval — that can reclassify rows and move rows back to the slower refresh regime when the test supports doing so.

Thus:

> **conservative runtime upgrade ≠ irreversible classification**.

The maintenance-policy state has its own lifecycle: create, observe, revise, and periodically revalidate.

### 6. Reducing refresh adds other maintenance work

AVATAR reduces unnecessary DRAM refreshes by adding or relying on:

- ECC storage/checking/correction;
- a row-refresh table;
- runtime class updates;
- periodic whole-memory scrubbing;
- infrequent retention testing.

The paper explicitly evaluates scrub-period tradeoffs: scrubbing more frequently improves the modeled reliability margin but consumes more energy and foreground bandwidth.

Therefore:

> **less refresh work ≠ less total maintenance work**.

The policy spends observation, correction, metadata, and revalidation work to avoid some restoration work.

## Reliability and evidence boundary

The paper derives long modeled times to uncorrectable error from its VRT characterization and architectural assumptions. Those values are useful inside the paper's comparison, but they must not be rewritten as years of demonstrated field operation.

Two boundaries are especially important.

First, the design/evaluation assumes that a scrub interval identifies the VRT-related data errors that arise during that interval. That assumption is part of the model closure; it is not direct proof that every real platform scrub catches every possible interval failure.

Second, the paper evaluates AVATAR as a research architecture. It does not identify a shipped commercial processor, controller, DIMM, or JEDEC feature that implements AVATAR.

Therefore:

> **model-level reliability result ≠ deployed hardware lifetime**

and:

> **online VRT adaptation ≠ commercial retention-aware-refresh deployment**.

## Failure and forgetting boundaries

The case adds several failure modes above raw capacitor leakage:

- **missed observation** — a vulnerable cold line is not demand-accessed before an error accumulates;
- **scrub-latency exposure** — vulnerability appears between proactive inspections;
- **uncorrectable coincidence** — multiple relevant errors can exceed the assumed ECC correction envelope before policy catches up;
- **misclassification** — a non-VRT correctable error can cause an unnecessary fast-refresh upgrade;
- **stale policy state** — a row remains in a class no longer justified by its current behavior;
- **retest cost** — stronger revalidation consumes time/energy/bandwidth;
- **model-assumption failure** — actual VRT/error processes can depart from the fitted injection/coverage assumptions.

This extends Case 40's result. Retention failure can arise from a stale model of the substrate, but a feedback mechanism that revises the model is itself only as good as its observation coverage, correction envelope, and revalidation schedule.

## Prior art and anti-anachronism

AVATAR is not treated here as the invention of retention-aware refresh.

Case 40 already grounds RAIDR (2012), which uses profiled row-retention heterogeneity to select refresh cadence, and RAIDR itself cites earlier mechanisms such as Smart Refresh (2007). The 2013 experimental study then establishes DPD/VRT as a challenge to static profiling assumptions.

AVATAR's narrower historical contribution is therefore kept in the authors' own 2015 problem setting: use ECC plus scrub-derived runtime failures to adapt a multirate refresh policy under VRT.

The project term `maintenance-policy feedback` is an engineering reconstruction. It is not substituted for the paper's historical vocabulary.

## Functional analogy and philosophical limit

A bounded functional analogy can compare AVATAR to feedback-based preventive maintenance: a system observes correctable degradation and revises how aggressively it services the affected component.

The analogy stops there. AVATAR is not evidence that DRAM “knows itself,” remembers in a human sense, or performs diagnosis in the philosophical sense.

A narrow conceptual pressure does follow from the mechanism:

> A technical system can preserve payload partly by retaining a fallible description of how the substrate tends to fail, then revising that description when the substrate produces new evidence.

And:

> The state that says **how to remember** may itself require periodic correction and revalidation.

These are engineering/philosophical interpretations of the documented design, not claims about the authors' philosophical intentions.

## Cross-case result

The DRAM decomposition now includes a feedback loop that Case 40 left open:

```text
physical retention behavior
    -> initial profiling
    -> retained row-refresh class
    -> runtime error manifestation
    -> ECC detection/correction
    -> policy update
    -> changed future refresh cadence
    -> proactive scrub coverage
    -> later profile revalidation
```

The key addition is that **second-order retention infrastructure is no longer static**. It can itself be revised as the first-order substrate reveals failures.

## Claim ledger

| Claim | Label | Evidence status |
| --- | --- | --- |
| AVATAR is a DSN 2015 research design for VRT-aware multirate DRAM refresh | H/P | original paper + bibliographic record |
| Experimental motivation uses observations from 24 DRAM chips | H/P | original abstract/introduction |
| Initial retention testing populates a Row Refresh Table | H/P | original design section |
| A correctable ECC event upgrades the affected row to Fast Refresh | H/P | original design section |
| Periodic scrub is used to inspect low-activity memory and can trigger the same upgrade path | H/P | original design section |
| The evaluated default scrub interval is 15 minutes | H/P | original design/evaluation; paper-specific parameter, not a DRAM standard |
| Infrequent retention testing can later downgrade rows | H/P | original design section |
| The paper assumes scrub identifies VRT-related errors arising during the scrub interval | H/P | explicit design/evaluation assumption |
| ECC correction and future refresh reclassification are separate operations | E | reconstruction from documented control path |
| Scrub and refresh are separate maintenance operations | E/A | mechanism comparison; paper uses both as distinct paths |
| RRT/classification state is second-order retention infrastructure | E | project reconstruction |
| Reported reliability/refresh-savings results are research evaluation results, not commercial field lifetime | H/E | evidence-scope boundary |
| AVATAR was commercially deployed | X | not established by the bounded sources |
| AVATAR invented retention-aware DRAM refresh | X | contradicted by earlier profiled-refresh prior art |

## Sources

### Primary

- Moinuddin K. Qureshi, Dae-Hyun Kim, Samira Manabi Khan, Prashant J. Nair, and Onur Mutlu, **“AVATAR: A Variable-Retention-Time (VRT) Aware Refresh for DRAM Systems,”** DSN 2015, pp. 427–437, DOI `10.1109/DSN.2015.58`. Author/lab-hosted PDF: <https://memlab.ece.gatech.edu/papers/DSN_2015_1.pdf>.
- Carnegie Mellon ISTC mirror of the same paper: <https://www.istc-cc.cmu.edu/publications/papers/2015/avatar-dram-refresh_dsn15.pdf>.

### Bibliographic / institutional

- Georgia Tech Memory Systems Lab publication listing for the DSN 2015 paper.
- DBLP bibliographic record for DSN 2015, pp. 427–437.

## Related repository check

`tmzncty/computing-archaeology` was searched for `AVATAR`, `VRT`, `RAIDR`, and retention-aware DRAM refresh before this slice. No dedicated AVATAR/RAIDR retention-refresh case was found. The broader semiconductor-memory engineering history still belongs there; this case keeps only the retention-specific feedback comparison.
'''

          evidence = r'''# Case 43 Grounding — AVATAR VRT-Aware DRAM Refresh (2015)

## Purpose

This record grounds [`../cases/43-avatar-vrt-aware-dram-refresh-feedback.md`](../cases/43-avatar-vrt-aware-dram-refresh-feedback.md) in the original 2015 AVATAR paper and records the limits needed to keep a research architecture, an experimental characterization, and a modeled reliability result from being collapsed into one deployment claim.

## Primary source

Moinuddin K. Qureshi, Dae-Hyun Kim, Samira Manabi Khan, Prashant J. Nair, and Onur Mutlu, **“AVATAR: A Variable-Retention-Time (VRT) Aware Refresh for DRAM Systems,”** 45th Annual IEEE/IFIP International Conference on Dependable Systems and Networks (DSN), 2015, pp. 427–437, DOI `10.1109/DSN.2015.58`.

Direct author/institution mirrors inspected:

- <https://www.istc-cc.cmu.edu/publications/papers/2015/avatar-dram-refresh_dsn15.pdf>
- <https://memlab.ece.gatech.edu/papers/DSN_2015_1.pdf>

Bibliographic cross-checks:

- Georgia Tech Memory Systems Lab publication listing;
- DBLP DSN 2015 record.

## Directly grounded historical claims

### Abstract / introduction — printed pp. 427–428

The paper states that multirate refresh depends on accurate retention-time profiles, while VRT allows cells to switch between retention states during runtime. The authors characterize VRT using **24 DRAM chips** and argue that continued runtime appearance of new VRT failures makes a static profile insufficient by itself.

The paper introduces AVATAR as a system-level mechanism that uses ECC plus multirate refresh and updates row refresh behavior when runtime VRT failures are detected.

**Boundary:** the experiment supplies measured VRT behavior; the long-term system reliability claims are derived through the paper's model/evaluation rather than decades of field observation.

### Design — printed pp. 433–434

The design path directly establishes:

1. retention testing creates the initial `Row Refresh Table` (`RRT`);
2. rows have slow/fast refresh classification;
3. data ECC detects and corrects runtime errors on accessed lines;
4. when a word encounters a correctable ECC event, AVATAR upgrades the row to `Fast Refresh`;
5. ordinary accesses do not cover cold memory, so AVATAR adds proactive memory scrub;
6. scrub errors feed the same row-upgrade path;
7. the evaluated scrub period is 15 minutes;
8. infrequent retention testing can later reclassify/downgrade rows.

The paper also notes that soft errors can cause a conservative row upgrade. This prevents a false inference that every ECC correction is a uniquely identified VRT event.

### Explicit model assumption — design/evaluation boundary

The authors explicitly assume that the scrub operation identifies the VRT-related data errors that occur during the scrub interval.

This assumption is recorded because it is a closure condition for the reported reliability analysis. It must not be rewritten as empirical proof that every implementation of a periodic scrub detects every interval failure.

### Evaluation — printed pp. 434–436

The paper evaluates reliability, refresh reduction, performance, and energy under the modeled VRT process and the AVATAR policy.

Useful bounded results include:

- refresh savings decrease as runtime upgrades accumulate and can be restored by infrequent retesting;
- shorter scrub intervals improve the modeled reliability margin but increase energy/bandwidth cost;
- the paper's default 15-minute scrub period is an evaluated design point, not an industry requirement.

The reported multi-decade / multi-century time-to-failure values are **model results under the paper's assumptions**. They are not deployed-hardware lifetimes and are not used in the case as field-compliance evidence.

## Historical vocabulary retained

The case keeps the paper's own terms where mechanism depends on them:

- `AVATAR`;
- `Variable-Retention-Time (VRT) Aware Refresh`;
- `Active-VRT Pool (AVP)`;
- `Active-VRT Injection (AVI)`;
- `Row Refresh Table (RRT)`;
- `SlowRefresh`;
- `Fast Refresh`;
- `Data ECC`;
- `Scrub`;
- `Retention Testing`.

Project terms such as `maintenance-policy feedback`, `second-order retention infrastructure`, `runtime policy repair`, and `policy-state lifecycle` are labeled engineering reconstruction rather than attributed to the 2015 authors.

## Evidence distinctions supported

### Historical record

- AVATAR is a DSN 2015 research proposal.
- It is motivated by VRT behavior measured on 24 DRAM chips.
- It combines initial profiling, RRT state, ECC, proactive scrub, row upgrades, multirate refresh, and infrequent retesting.
- Correctable errors can trigger a row upgrade to Fast Refresh.
- The evaluation uses a 15-minute scrub design point and an explicit scrub-coverage assumption.

### Engineering reconstruction

From those documented mechanisms, the repository may infer:

- `error correction != future-protection policy`;
- `demand-triggered checking != full-memory coverage`;
- `scrub != refresh`;
- `surviving profile != valid profile`;
- `policy update != immutable diagnosis`;
- `less refresh != less total maintenance work`;
- retained maintenance metadata can itself require revision/revalidation.

### Functional analogy

AVATAR may be compared to feedback-based preventive maintenance only at the relation level: observed correctable degradation changes later maintenance intensity.

No historical genealogy to non-DRAM maintenance systems follows from that analogy.

### Philosophical interpretation

The case can pressure a narrow proposition: a system may retain not only payload but also revisable state describing how aggressively the payload's substrate must be maintained.

This is not evidence that the authors formulated a philosophy of memory or that DRAM possesses human-like self-knowledge.

## Prior-art boundary

Case 40 already grounds RAIDR (2012) and the 2013 DPD/VRT experimental challenge. RAIDR itself cites earlier refresh-reduction mechanisms including Smart Refresh (2007).

Therefore the supported historical claim is narrow:

> AVATAR addresses VRT-aware runtime adaptation of a multirate-refresh policy through ECC/scrub feedback in the 2015 research design.

Unsupported upgrades include:

- “AVATAR invented retention-aware refresh”;
- “AVATAR was the first DRAM refresh optimization”;
- “AVATAR was commercially deployed”;
- “AVATAR solves all DPD/VRT profiling uncertainty.”

## Inspection note

Page-resolved PDF text for the original paper was directly inspected from both institutional mirrors. The design figure's labels were available in the extracted PDF text. An automated PDF screenshot endpoint was attempted during this research slice but did not return a renderable image; consequently no claim in the case depends uniquely on visual interpretation of the figure.

## Related-repository check

`tmzncty/computing-archaeology` was searched before writing for AVATAR/VRT/RAIDR retention-refresh coverage. No dedicated case was found. This record therefore adds only the retention-policy feedback relation rather than duplicating a generic DRAM history.
'''

          readme_p = Path('README.md')
          road_p = Path('ROADMAP.md')
          index_p = Path('CASE_INDEX.md')
          readme = readme_p.read_text()
          road = road_p.read_text()
          index = index_p.read_text()

          case42_readme = "- [`cases/42-apache-kafka-log-compaction-delete-marker-retention.md`](cases/42-apache-kafka-log-compaction-delete-marker-retention.md) — grounded compacted-changelog bridge: Kafka 0.8.1 keeps at least the last known value per key while background segment recopy removes superseded history; original logical offsets remain permanent traversal positions even when records disappear, and time-bounded delete markers make consumer catch-up progress part of the deletion-observation contract."
          case43_readme = "- [`cases/43-avatar-vrt-aware-dram-refresh-feedback.md`](cases/43-avatar-vrt-aware-dram-refresh-feedback.md) — grounded VRT-aware DRAM refresh-feedback bridge: AVATAR combines initial row profiling with ECC-corrected runtime failure evidence and proactive scrubbing to upgrade vulnerable rows into a fast-refresh class, then periodically re-tests policy state; correcting an observed error, covering cold memory, and changing future refresh cadence remain separate retention work."
          ev42_readme = "- [`evidence/42-kafka-081-log-compaction-grounding.md`](evidence/42-kafka-081-log-compaction-grounding.md) — Case-42 grounding record: versioned Apache Kafka 0.8.1 design documentation and exact release-tag cleaner/segment source separate current-state reconstruction from complete history, stable logical offsets from record/byte-position survival, background recopy from in-place erase, and bounded delete-marker observation from Cassandra/Swift-style replica-repair windows."
          ev43_readme = "- [`evidence/43-avatar-2015-vrt-aware-refresh-grounding.md`](evidence/43-avatar-2015-vrt-aware-refresh-grounding.md) — Case-43 grounding record: the DSN 2015 AVATAR paper separates initial row profiling, runtime ECC correction, proactive scrub coverage, row-level fast/slow refresh reclassification, periodic retesting, and modeled reliability from commercial deployment; the paper's explicit scrub-coverage assumption is retained as an evidence limit."
          assert case42_readme in readme
          assert ev42_readme in readme
          readme = readme.replace(case42_readme, case42_readme + '\n' + case43_readme, 1)
          readme = readme.replace(ev42_readme, ev42_readme + '\n' + ev43_readme, 1)

          assert 'partially advanced by seven grounded bounded sub-slices' in road
          road = road.replace('partially advanced by seven grounded bounded sub-slices', 'partially advanced by eight grounded bounded sub-slices', 1)
          road_anchor = "The broad item stays unchecked because a true JEDEC standards chronology"
          assert road_anchor in road
          case43_road = "[`cases/43-avatar-vrt-aware-dram-refresh-feedback.md`](cases/43-avatar-vrt-aware-dram-refresh-feedback.md), grounded by [`evidence/43-avatar-2015-vrt-aware-refresh-grounding.md`](evidence/43-avatar-2015-vrt-aware-refresh-grounding.md), turns the Case-40 VRT/profile-validity problem into an online feedback regime: ECC-corrected runtime failures and proactive scrub coverage can upgrade rows to `Fast Refresh`, while infrequent retention re-testing can later downgrade them; detection, correction, future cadence, scrub coverage, and profile revalidation remain separate relations. "
          road = road.replace(road_anchor, case43_road + road_anchor, 1)
          open_gap = 'commercial retention-aware-refresh deployment, online VRT/DPD-aware profile revalidation or ECC composition, and RowHammer-oriented refresh policy'
          assert open_gap in road
          road = road.replace(open_gap, 'commercial retention-aware-refresh deployment, broader DPD-complete profiling and product-level ECC/refresh composition, and RowHammer-oriented refresh policy', 1)

          case43_row = "| [AVATAR VRT-Aware DRAM Refresh: ECC Feedback, Scrubbing, and Runtime Row Reclassification](cases/43-avatar-vrt-aware-dram-refresh-feedback.md) | **grounded** | volatile dynamic payload + initial row-retention profile/RRT + SECDED-style ECC assumption + runtime correctable-error observations + proactive scrub + fast/slow refresh reclassification + periodic retesting | separate error correction from future-protection policy; demand detection from cold-memory coverage; static profile survival from runtime policy revision; refresh savings from added scrub/retest work | [2015 AVATAR grounding](evidence/43-avatar-2015-vrt-aware-refresh-grounding.md); commercial deployment, DPD-complete profiling, product-level ECC semantics, RowHammer policy, and later profiler evolution remain separate work |"
          cases_marker = '\n\n---\n\n## Comparison matrix — provisional'
          assert cases_marker in index
          index = index.replace(cases_marker, '\n' + case43_row + cases_marker, 1)

          compare43 = "| AVATAR VRT-aware DRAM refresh / 2015 bounded research regime | dynamic-cell charge + Row Refresh Table class state + ECC correction evidence + scrub/retest schedule | initial retention test populates RRT; demand ECC or periodic scrub detects/corrects an error and upgrades the whole row to Fast Refresh; infrequent retest can later restore SlowRefresh status | demand access checks touched lines while explicit scrub checks cold memory; refresh is a separate restoration operation whose cadence is changed by observed error evidence | row address + retained RRT fast/slow class; correction events can update the maintenance policy attached to that row | fixed DRAM row; embodiment does not migrate, but the retained maintenance classification changes during runtime | no application history; RRT/error-derived class state is second-order retention infrastructure and its validity is maintained through feedback/retest |"
          cross_marker = '\n\n---\n\n## Cross-case findings already supported'
          assert cross_marker in index
          index = index.replace(cross_marker, '\n' + compare43 + cross_marker, 1)

          old40 = 'commercial deployment, online VRT-aware profiling/ECC, JEDEC standardization, and RowHammer-oriented refresh remain separate work'
          assert old40 in index
          index = index.replace(old40, 'runtime VRT-aware ECC/scrub reclassification is now handled separately in Case 43; commercial deployment, DPD-complete profiling, JEDEC standardization, and RowHammer-oriented refresh remain separate work', 1)

          finding400 = "400. **source-of-current-state role ≠ point-in-time archive role** — a compacted Kafka topic can remain a source from which current keyed state is rebuilt while no longer retaining enough superseded records to reproduce arbitrary historical points; `source of truth` for current state does not mean complete historical archive."
          assert finding400 in index
          findings = r'''
401. **profile survival ≠ policy validity, and runtime fault evidence can revise maintenance policy** — AVATAR does not trust the initial row profile as an immutable future truth: a correctable runtime error can change the affected row's retained refresh classification.
402. **error correction ≠ future protection** — ECC restores the currently observed logical word, while the subsequent row upgrade changes how aggressively the row will be refreshed in the future; repair of one manifestation and revision of later maintenance are separate actions.
403. **demand-triggered ECC observation ≠ full-memory coverage** — ordinary accesses expose errors only in touched lines, so AVATAR adds proactive scrub to inspect cold memory before treating runtime feedback as sufficiently broad.
404. **memory scrub ≠ DRAM refresh** — scrub reads/checks/corrects and supplies classification evidence, whereas refresh restores dynamic-cell charge according to the resulting cadence; the two forms of maintenance compose without becoming one mechanism.
405. **ECC correction event ≠ certain VRT diagnosis** — the paper notes that soft errors can also trigger conservative row upgrades, so a safe policy response need not imply perfect causal classification of the observed fault.
406. **conservative runtime upgrade ≠ irreversible classification** — AVATAR can accumulate Fast-Refresh upgrades after errors, then use infrequent retention re-testing to downgrade rows again; second-order maintenance state has a revision lifecycle rather than one permanent label.
407. **less refresh work ≠ less total maintenance work** — refresh savings are purchased with ECC checking/correction, RRT state, periodic whole-memory scrub, runtime policy updates, and infrequent re-testing; optimization redistributes maintenance rather than abolishing it.
408. **model-level reliability result ≠ deployed hardware lifetime** — AVATAR's long time-to-failure estimates combine 24-chip VRT characterization with an architecture-level model and assumptions; they are not decades of commercial field observation.
409. **scrub-coverage assumption ≠ demonstrated universal detection** — the paper explicitly assumes that scrub identifies VRT-related errors arising within its interval; this closes the evaluation model but does not prove that every real scrub implementation catches every possible interval failure.
410. **online VRT adaptation ≠ commercial retention-aware-refresh deployment** — the 2015 paper grounds a research architecture and evaluation, not a named shipped memory controller, processor, DIMM, or JEDEC feature implementing AVATAR.
411. **retention-policy state can itself require revalidation** — the Row Refresh Table is retained control state used to decide future maintenance, yet runtime upgrades and later re-testing show that this second-order state must itself be kept current enough to remain safe and efficient.'''
          index = index.replace(finding400, finding400 + '\n' + findings, 1)

          assert index.count('forty-three') >= 2
          index = index.replace('forty-three', 'forty-four')
          inventory_old = 'Cassandra tombstone/repair-window, and Kafka log-compaction/delete-marker-retention bridges;'
          assert inventory_old in index
          index = index.replace(inventory_old, 'Cassandra tombstone/repair-window, Kafka log-compaction/delete-marker-retention, and AVATAR runtime-VRT-aware ECC/scrub refresh-feedback bridges;', 1)

          Path('cases/43-avatar-vrt-aware-dram-refresh-feedback.md').write_text(case)
          Path('evidence/43-avatar-2015-vrt-aware-refresh-grounding.md').write_text(evidence)
          readme_p.write_text(readme)
          road_p.write_text(road)
          index_p.write_text(index)

          assert 'cases/43-avatar-vrt-aware-dram-refresh-feedback.md' in readme
          assert 'evidence/43-avatar-2015-vrt-aware-refresh-grounding.md' in readme
          assert 'eight grounded bounded sub-slices' in road
          assert 'online VRT/DPD-aware profile revalidation or ECC composition' not in road
          assert '411. **retention-policy state can itself require revalidation**' in index
          assert 'After forty-four bounded cases, **all forty-four cases are now `grounded`.**' in index
          assert 'currently forty-four' in index
          PY

      - name: Commit integrated case
        shell: bash
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add README.md ROADMAP.md CASE_INDEX.md cases/43-avatar-vrt-aware-dram-refresh-feedback.md evidence/43-avatar-2015-vrt-aware-refresh-grounding.md
          git diff --cached --check
          git commit -m 'case: add grounded AVATAR VRT-aware refresh bridge'
          git push

      - name: Remove one-shot workflow
        if: success()
        shell: bash
        run: |
          git rm .github/workflows/automation-case43-avatar.yml
          git commit -m 'automation: remove Case 43 integration workflow'
          git push
