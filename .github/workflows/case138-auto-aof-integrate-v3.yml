name: integrate-case138-auto-aof-genealogy-v3

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/case138-auto-aof-integrate-v3.yml'

permissions:
  contents: write

jobs:
  integrate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Apply bounded research updates
        run: |
          python3 .case138_integrate.py
          rm -f .github/workflows/case138-auto-aof-integrate-v3.yml
      - name: Commit and push canonical tree
        shell: bash
        run: |
          set -euo pipefail
          git config user.name 'technical-retention-bot'
          git config user.email 'technical-retention-bot@users.noreply.github.com'
          git add -A
          git status --short
          git commit -m 'case138: deepen automatic AOF rewrite genealogy [skip ci]'
          git push origin HEAD:main
