name: deepen-case112-arfm

on:
  push:
    paths:
      - '.github/workflows/integrate-case112-arfm.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  integrate:
    runs-on: ubuntu-latest
    concurrency:
      group: technical-retention-research
      cancel-in-progress: false
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: main
      - name: Configure Git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
      - name: Deepen Case112 adaptive refresh management
        run: |
          bash scripts/integrate-case112-arfm.sh
