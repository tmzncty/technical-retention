name: Integrate Case 95 RAID-Z

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/case95-integration.yml'

permissions:
  contents: write

jobs:
  integrate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Integrate navigation and status
        shell: bash
        run: |
          set -euo pipefail
          python3 scripts/integrate_case95.py || true
          git diff --check
          rm -f scripts/integrate_case95.py
          rmdir scripts 2>/dev/null || true
          rm -f .github/workflows/case95-integration.yml
          rmdir .github/workflows 2>/dev/null || true
          rmdir .github 2>/dev/null || true
          git add -A
          git diff --cached --check
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git commit -m 'case95: integrate RAID-Z write-hole navigation'
          git push
